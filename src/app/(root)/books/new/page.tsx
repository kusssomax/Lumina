import { UploadForm } from "@/components/UploadForm";

const Page = () => {
  return (
    <section className="flex flex-col items-center w-full">
      <UploadForm />
    </section>
  );
};

export default Page;
