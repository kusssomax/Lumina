export const MAX_PDF_BYTES = 50 * 1024 * 1024

/** Optional safety cap when user provides a cover image */
export const MAX_COVER_BYTES = 10 * 1024 * 1024

export const MAX_PDF_MB = MAX_PDF_BYTES / (1024 * 1024)
